# Xcode .gitignore
####################

# Xcode temporary files that should not be committed
xcuserdata/
*.xccheckout
*.xcscmblueprint

# Xcode SPM integration (Xcode 11+)
# The Package.resolved file should be committed to your repository to ensure reproducible builds
# However, the workspace checks contain user-specific data and should not be committed
*.xcworkspace/xcshareddata/swiftpm/
*.xcworkspace/xcshareddata/WorkspaceSettings.xcsettings

# Derived data and build output
DerivedData/
Build/
*.pbxuser
!default.pbxuser
*.mode1v3
!default.mode1v3
*.mode2v3
!default.mode2v3
*.perspectivev3
!default.perspectivev3
xcuserdata/
*.xcuserstate
project.xcworkspace/

# Other files and directories to ignore
.DS_Store
.AppleDouble
.LSOverride

# Thumbnails
._*

# Files that might appear in the root of a volume
.DocumentRevisions-V100
.fseventsd
.TemporaryItems
.Trashes
.VolumeIcon.icns
.com.apple.timemachine.donotpresent

# Directories potentially created on remote AFP share
.AppleDB
.AppleDesktop
Network Trash Folder
Temporary Items
.apdisk

# CocoaPods
Pods/
# Uncomment the next line if you're using a more recent version of CocoaPods which no longer recommends committing the Pods directory
# /Pods/

# Carthage
Carthage/Build

# fastlane
fastlane/report.xml
fastlane/Preview.html
fastlane/screenshots
fastlane/test_output

# Swift Package Manager
.build/
.swiftpm/xcode/package.xcworkspace
.swiftpm/xcode/xcuserdata

# Yarn / npm package directories
node_modules/
yarn.lock

# OS X Finder
.DS_Store

# User-specific files
*.moved-aside
*.xcuserstate

# Ignore personal signing config
signing.xcconfig

